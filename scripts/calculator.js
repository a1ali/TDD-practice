class Calculator { 
    // object that contains the basic operations: add, subtract, divide, and multiply.
    add(a, b) { return a + b };
    subtract(a, b) {return a - b};
    multiply(a, b) {return a * b};
    divide(a, b) {return a / b};
}

module.exports = Calculator;